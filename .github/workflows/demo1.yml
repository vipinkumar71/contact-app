name: dev environment

on: push

jobs:
  my-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: my-step
        run: echo "Hello World!"
